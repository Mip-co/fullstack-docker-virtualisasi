events { worker_connections 1024; }

http {
 upstream api_backend {
  server api:3000;
 }

 server {
  listen 80;

  location /api/ {
   proxy_pass http://api_backend/;
  }

  location / {
   return 200 'Docker Full-Stack Running!';
   add_header Content-Type text/plain;
  }
 }
}
